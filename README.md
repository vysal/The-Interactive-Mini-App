# The-Interactive-Mini-App

A small React + TypeScript product catalog app built with Vite.

## Features
- Render a product grid from data
- Toggle to show only in-stock items
- Show sale count badge
- Add new products with validation
- Reject empty or invalid price values

## Scripts
- `npm install`
- `npm run dev`
- `npm run build`
